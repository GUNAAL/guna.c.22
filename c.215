+#include<stdio.h>
+#include<conio.h>
+int main()
+{
+chr c;
+print f("enter a charecter:");
+scan f("%c",&c);
+if(c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u' || c == 'A' || C == 'E' || C == 'I' || C== 'O' || C == 'U' );
+{
+print f ("%c is a vowel\n",c);
+}
+else ();
+{
+print f("%c is a consonent\n",c);
+}
+getch ();
+return 0;
+}
